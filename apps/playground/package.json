{
  "name": "playground",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm build && serve .",
    "build": "esbuild index.js --bundle --minify --format=iife --outfile=dist/app.min.js && cp node_modules/pdfjs-dist/build/pdf.worker.min.mjs dist/"
  },
  "dependencies": {
    "@herablog/built-in-ai": "workspace:*",
    "jszip": "3.10.1",
    "marked": "18.0.2",
    "pdfjs-dist": "5.6.205"
  },
  "devDependencies": {
    "esbuild": "0.28.0",
    "serve": "14.2.6"
  }
}
